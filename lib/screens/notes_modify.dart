import 'package:flutter/material.dart';

class NotesModify extends StatelessWidget {
  const NotesModify({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Notes'),
      ),
      body: Container(),
    );
  }
}
